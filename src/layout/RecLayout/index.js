import { Outlet } from 'react-router-dom';

// project imports

// ==============================|| RecLayout LAYOUT ||============================== //

const RecLayout = () => (
  <>
    <Outlet />
  </>
);

export default RecLayout;
